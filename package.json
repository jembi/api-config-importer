{
  "name": "importer",
  "version": "1.0.0",
  "description": "A generic config importer which makes an API call to configure services running in a Docker swarm.",
  "repository": "https://github.com/jembi/api-config-importer",
  "main": "openhimConfig.js",
  "scripts": {
    "start": "node configBase.js",
    "build": "esbuild configBase.js --bundle --platform=node --target=node10.4 --outfile=config.js"
  },
  "author": "",
  "license": "Apache",
  "dependencies": {
    "axios": "^0.26.1",
    "esbuild": "^0.14.36",
    "form-data": "^4.0.0"
  }
}
